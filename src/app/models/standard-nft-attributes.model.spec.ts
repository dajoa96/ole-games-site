import { StandardNftAttributes } from './standard-nft-attributes.model';

describe('StandardNftAttributes', () => {
  it('should create an instance', () => {
    expect(new StandardNftAttributes()).toBeTruthy();
  });
});
