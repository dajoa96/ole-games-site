import { StandardToastConfig } from './standard-toast-config.model';

describe('StandardToastConfig', () => {
  it('should create an instance', () => {
    expect(new StandardToastConfig()).toBeTruthy();
  });
});
