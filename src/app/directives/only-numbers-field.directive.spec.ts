import { OnlyNumbersFieldDirective } from './only-numbers-field.directive';

describe('OnlyNumbersFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumbersFieldDirective();
    expect(directive).toBeTruthy();
  });
});
